# Future Bootstrap Improvements (TODO)

Here are high-priority quality-of-life enhancements and tools to consider exploring when bootstrapping new machines in the future:

---

## 🛠️ Proposed Enhancements

### 1. Vim Syntax Plugins & Productivity Helpers
- **C++/Arduino Syntax Highlighting**: Improve editor experience for `.ino` and `.pde` files.
- **`vim-commentary`**: Add a lightweight, single-file plugin to allow quick line commenting (e.g. via `gcc` mapping).
- **Go/Python syntax updates**: Enhance standard built-in vim editing experience.

### 2. GitHub CLI (`gh`) Integration
- **Auto-authentication**: Use `gh auth login` to automate machine authorization.
- **Auto-SSH Setup**: The GitHub CLI can automatically generate, configure, and upload SSH keys to GitHub profiles directly from the command line, making fresh key setup completely touchless.
